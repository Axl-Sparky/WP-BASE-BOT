const {
	serialize
} = require('./serialize');
const {
	message
} = require('./message');
const {
	bot,
	modules
} = require('./modules');
const {
	database,
	configurator,
	processMedia,
	timeUtils,
	isUrl,
	language,
	formatp
} = require('./core');

module.exports = {
	serialize,
	message,
	bot,
	modules,
	database,
	configurator,
	processMedia,
	timeUtils,
	isUrl,
	language,
	formatp
};
